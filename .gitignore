# Binary
/tidybill
/tidybill.exe
/tidybill-linux
/seed
/main

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Go
vendor/

# Database
*.db
*.sqlite

# Generated files
*.pdf

# rclone sidecar cache (binaries fetched by scripts/fetch-rclone.sh, not committed)
.rclone-cache/

# rclone sidecar binaries — fetched per-platform via scripts/fetch-rclone.sh,
# never committed. ~76 MB each (rclone is statically linked with all 70+
# backend SDKs, hence the size).
desktop/src-tauri/binaries/rclone-*
